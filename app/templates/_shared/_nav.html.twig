<nav class="navbar navbar-expand-md bg-blue" data-bs-theme="dark">
  <div class="container-fluid">
    <a href="{{path('app_home')}}">
      <img src="{{ asset('image/logo2.png') }}" class="" style="max-height: 40px;" alt="AssuMotor">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
      aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto mx-auto">
        <li class="nav-item mx-1">
          <a class="nav-link " href="{{path('app_home')}}">Accueil
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item mx-1">
          <a class="nav-link " href="{{path('app_quote')}}">Devis</a>
        </li>
        <li class="nav-item mx-1">
          <a class="nav-link" href="#">Apropos</a>
        </li>

        {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item mx-1">
            <form action="{{path('app_admin_dashboard')}}" method="get">
              <input type="submit" class="nav-link" value="🗝️Espace Admin" class="text-primary">
            </form>
          </li>
        {% endif %}

        {% if app.user %}
          <li class="nav-item mx-1">
            <form action="{{path('app_logout')}}" method="post">
              <input type="submit" class="nav-link" value="Déconnexion">
            </form>
          </li>
        {% else %}
          <li class="nav-item mx-1">
            <a class="nav-link" href="{{path('app_login')}}">Connexion</a>
          </li>
        {% endif %}
      </ul>
      {# Espace perso #}
      <ul class="navbar-nav me-0 ">
        <li class="nav-item dropdown mx-1 ">
          <a class="nav-link dropdown-toggle " data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true"
          aria-expanded="false">
          <img src="{{ asset('image/profil.png') }}" class="" style="max-height: 24px;" alt="AssuMotor">
          </a>
          <div class="dropdown-menu dropdown-menu-end bg-blue">
            {% if app.user %}
            <a class="dropdown-item text-logo" href="{{ path('app_account_user') }}">Profil</a>
              {% if is_granted('ROLE_ADMIN') %}
                <a class="dropdown-item text-logo" href="{{ path('app_admin_subscription_index') }}"> Gestion souscriptions🔐</a>
                <a class="dropdown-item text-logo" href="{{ path('app_admin_user_index') }}"> Gestion utilisateurs🔐</a>
              {% endif %}
            <a class="dropdown-item text-logo" href="{{ path('app_account_quote') }}">Devis</a>
            <a class="dropdown-item text-logo" href="{{ path('app_account_subscription') }}">Demandes Souscription</a>
            <div class="dropdown-divider"></div>
            <form action="{{path('app_logout')}}" method="post">
              <input type="submit" class="dropdown-item text-logo" value="Déconnexion">
            </form>
            {% else %}
            <a class="dropdown-item text-logo" href="{{path('app_login')}}">Connexion</a>
            <a class="dropdown-item text-logo" href="{{path('app_register')}}">Enregistrement</a>
            {% endif %}
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>